// Program-manager view of the commercial-site directory. Identical surface to
// the admin page — the page body is shared so the two never diverge. Access is
// gated by /program/layout.tsx (program_manager | admin).
export { default } from '@/app/admin/commercial-accounts/page';
