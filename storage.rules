rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default-deny. Real rules land in Phase 11 (doorstep photos, etc).
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
